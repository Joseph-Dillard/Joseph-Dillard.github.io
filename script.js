document.addEventListener("DOMContentLoaded", () => {
    setTimeout(() => {
        document.querySelector(".nav-bar").classList.add("show-nav");
    }, 3000); // Matches the name animation duration
});